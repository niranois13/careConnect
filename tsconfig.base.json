{
  "compilerOptions": {
    // Target ES2022 pour utiliser les features modernes (Node.js 18+ compatible)
    "target": "ES2022",
    // Utilisation des modules ECMAScript (ESM), recommandé pour Node >= 16+ et Vite
    "module": "NodeNext",
    // Résolution des modules comme Node (import/export natifs)
    "moduleResolution": "NodeNext",
    // Librairies globales disponibles dans le code (DOM pour React, ES2022 pour Node)
    "lib": ["ES2022", "DOM"],
    // Active tous les contrôles stricts (null, type, etc.)
    "strict": true,
    // Vérifie que les noms de fichiers respectent la casse, utile sur Linux
    "forceConsistentCasingInFileNames": true,
    // Isolation des modules (utile pour `tsc --build` et Vite)
    "isolatedModules": true,
    // Interopérabilité avec des modules CommonJS (comme `jsonwebtoken`)
    "esModuleInterop": true,
    // Permet d'importer des fichiers .json
    "resolveJsonModule": true,
    // Ne pas vérifier les types des dépendances (souvent inutile, mais à réévaluer)
    "skipLibCheck": true,
    // Permet d'importer aussi des fichiers .js (utile pour migration ou libs JS)
    "allowJs": true,
    // Ne rien émettre (ce sont les projets enfants qui compilent)
    "noEmit": true,
    // Racine des fichiers sources (utile pour `rootDirs`)
    "rootDir": ".",
    // Base des imports absolus (`@/` par exemple)
    "baseUrl": ".",
    // Permet d'importer avec extensions .ts explicites si besoin
    "allowImportingTsExtensions": true
  }
}
